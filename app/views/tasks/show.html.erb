<h1><%= @task.title %></h1>

<p><%= @task.details %></p>

<p>Still to do: <%= @task.completed %></p>

<p><%= link_to "Edit the task", edit_path  %><br>

<%= link_to 'Delete', task_path(@task), method: :delete, data: {confirm: 'Are you sure you want to delete?'} %>

<p><%= link_to "Back to tasks", root_path  %>
